# 05 — 문서와 주장 문구

담당 범위: `documents/spin-glass-qml-report.md`, `documents/report.tex`, 그리고
"우리가 정확히 무엇을 주장하는가"의 문장 관리.

## 핵심 논지 한 문장

BP-PPS로 고전 훈련한 얕은 파라미터 회로가, 같은 정확도에서 Trotter보다 얕고,
고전 SPD가 무너지기 시작하는 시간 구간에서도 하드웨어 예산 안에 들어가는
**창(window)** 이 존재한다.

> ⚠️ **이 문장은 09-03 기준으로 데이터와 어긋나기 시작했습니다.** 앞부분("Trotter
> 보다 얕다")은 `L=3`, `T=0.5` 에서 23% 절감이라는 얇은 형태로만 살아 있고,
> 뒷부분("하드웨어 예산 안")은 샘플링에 대해서는 거짓입니다 (10×10 `t=2`,
> `ε=3e-3` 에서 `F=0.013`). 반대로 여기 안 적힌 목표 3이 가장 강해졌습니다 —
> 단열 대비 10배 이상. **아래 "2026-09-03 (3)" 절의 (A)/(B) 를 정할 때 이
> 문장도 같이 다시 씁니다.** `L=5` 결과 전에는 건드리지 마세요.

## 이미 고친 것 — 되돌리지 말 것

**부호 문제 서술을 제거했습니다.** 이 해밀토니안은 어떤 `J` 에 대해서도
stoquastic 이라 부호 문제가 없습니다. frustration 은 있지만 그것과 부호 문제는
다른 것입니다. 자세한 근거는 `02-comparison-models.md`.

바닥상태 준비를 **"고전적으로 불가능"이 아니라 "회로 깊이 우위"** 로 재구성
했습니다. QMC가 이 모델의 바닥상태를 샘플링할 수 있으므로 그쪽으로는 주장이
서지 않습니다.

## 결론 문장 작성 요령 — 못 찾았다고 쓰는 것도 결과

> 이 절은 원래 `RUNBOOK.md` §4 에 있었습니다. `ac21b3a` 가 RUNBOOK 을
> "지금 돌릴 것"만 남기고 줄이면서 삭제됐는데, 이 파일과
> `01-scale-plan.md` 에서 **네 번 인용되고 있었습니다.** 주장 문구에 관한
> 규칙이므로 이쪽이 제자리입니다. RUNBOOK 을 다시 가리키지 마세요.

세 조건이 겹치는 구간을 찾았다면 그렇게 쓰고, **못 찾았다면 못 찾았다고
쓰세요.** 못 찾은 경우 그 자체가 중요한 결과이며, **어느 조건이 먼저
깨졌는지가 다음 방향을 결정합니다.**

- (c) 가 먼저 깨짐 → 안수 표현력 부족. 층 수 / 게이트 종류를 늘려야 함
- (a) 가 안 깨짐 → SPD 가 너무 잘 버팀. `T` 를 더 늘리거나 모델을 바꿔야 함
- (b) 가 먼저 깨짐 → 압축률이 부족. 직접 훈련 스케일링을 개선해야 함

이 규칙이 실제로 적용된 곳: 목표 2 (`t*` 미측정이라 **주장하지 않음**),
"초선형 깊이" (측정 대상이 틀려 **인용 금지**), 목표 ①의 `L=4` 판정 기준
(`RUNBOOK.md` §2-1 — "지면 그대로 쓰는 것도 결과").

## 지금 데이터로 쓸 수 있는 것 / 없는 것

| 문장 | 근거 | 상태 |
|:--|:--|:--|
| 4×4에서 시간 진화 fidelity 0.99918 | `validation_results.json` | ✅ 쓸 수 있음 |
| 절단 오차 추정이 실제 오차를 상한 | `00_validate_small.py` TEST 12 | ⚠️ **범위를 붙여서만.** T1 의 `T=0.5` 에서 추정은 1.87e-4 이고 총 `L_XZ` 는 0.0149 이라 총손실을 상한하지 않습니다. 추정과 같은 자릿수인 것은 **infidelity 8.18e-4** 쪽입니다 |
| HVA가 같은 게이트 수에서 Trotter보다 정확하다 | `state_averaged*.json` (09-03) | ✅ **`L=3` 에서 `T`=0.5·1.0·2.0 모두 성립.** 곱상태 평균 기준 **2.86× / 1.23× / 1.20×**. `L=2` 는 세 시각 모두 패배 |
| 그 우위가 `T` 와 함께 유지된다 | 같은 파일 | ⚠️ **줄어듭니다.** 2.86 → 1.23 → 1.20. `T`=2.0 에서도 이기지만 마진이 얇고, `T`>2 는 안 쟀습니다 |
| `\|0…0>` 기준으로도 이긴다 | 같은 파일 | ❌ **아니오.** `T`≥1.0 에서는 `\|0…0>` 채점이면 Trotter 가 이깁니다. **채점 상태를 반드시 명시**하세요 |
| HVA 회로가 Trotter보다 **깊이가** 얕다 | `06_depth_comparison.png` | ❌ **쓰지 마세요.** 같은 스케줄러로 재면 1.07×, 2Q 깊이는 12 대 12로 동일. 4.7× 는 스케줄링 비교의 불공정이었습니다 (`01-scale-plan.md`) |
| 바닥상태 준비 우위 | 3층 `F0=0.7211`, 격차 0.4729 | ❌ **아직 안 됨** — 이유가 바뀌었습니다. 훈련은 안수 상한에 붙었고(2.5e-4), 막힌 것은 **경쟁 회로 곡선이 없다**는 점입니다 |
| 격차 0.4729 는 BP-PPS 의 한계다 | `gs_L5_aborted.json` | ❌ **아니라고 쓰세요.** 3층 안수의 한계입니다. 5층은 Adam 만으로 **0.2796** (41% 감소) |
| BP-PPS 훈련이 안수 ceiling 에 도달한다 | `report_goal3_layers.png` | ⚠️ **`L=3` 한정.** 위 단열 비교의 큰 배수들이 전부 여기 걸려 있습니다 — 유일하게 확인된 깊이입니다. `L=5` 는 ceiling 0.0419 대비 0.2796 으로 6.7배 미달 — 단 그 실행은 41시간 만에 중단돼 **미완 최적화의 하한**이지 도달 실패의 증거가 아닙니다 |
| 반집중·얽힘을 회로가 학습했다 | `report_goal2_hardness.png` | ❌ **쓰지 마세요.** 목적함수는 `L_XZ` 뿐이라 반집중에 훈련 압력이 없습니다. `L=2` 와 `L=3` 곡선이 겹치는 것이 증거 — **타겟 상태의 성질**이지 안수의 성질이 아닙니다 |
| BP-PPS 회로가 입력 상태에 강건하다 | `state_averaged.json` | ✅ **쓸 수 있습니다.** `\|0…0>` 대비 무작위 곱상태 평균 열화가 BP-PPS **1.08배**, grouped `S₂` 1.32배, `S₄` 1.39~1.83배. 작지만 실측이고 **연산자를 근사한다는 것의 직접 증거**입니다 |
| 바닥상태를 단열보다 얕은 회로로 만든다 | `gs_adiabatic.json` | ✅ **`L=3` 은 쓸 수 있습니다.** 실제 BP-PPS 72 2Q (격차 0.4729, `F0` 0.7211) 가 단열 **768 2Q** (0.6651, 0.6828) 를 양쪽 지표에서 이깁니다 — 10배 이상, 양쪽 다 실측 |
| 단열 대비 우위가 깊이에 따라 커진다 | 위 + ceiling | ⚠️ **외삽입니다.** 격차가 단열 `2Q^-0.94`, HVA ceiling `2Q^-3.60`. 같은 에너지 기준 5×(`L=2`) → 15×(`L=3`) → 121×(`L=5`) → 253×(`L=8`) 인데 `L≥3` 은 단열 측정 범위(768 2Q) 밖이고, **`L≥4` 는 BP-PPS 가 그 ceiling 에 도달한다는 증거도 없습니다** |
| 노이즈가 있어도 샷을 늘리면 샘플링이 된다 | `report_noise_budget.png` (c) | ❌ **관측량에만 참입니다.** `<O>` 는 `F` 로 되돌리고 `1/F²` 샷을 내면 되지만 **분포에는 되돌릴 인자가 없습니다.** 10×10 `t=2`, `ε=3e-3` 이면 `F=0.013` — 비트스트링 99%가 노이즈이고 샷으로 못 고칩니다. **목표 2가 샘플링 주장**이라는 점이 핵심 |
| T에 대한 깊이 스케일링이 sublinear | statevector pilot | ⚠️ **판정 보류 (09-03 강등).** "필요 층수 2→3→8→도달못함" 은 **`\|0…0>` 한 상태에 각도를 맞춘 pilot** 에서 나왔습니다 — `a45a88a` 가 철회한 바로 그 최적화입니다. 숫자를 인용하지 마세요. 단일상태 맞춤이 연산자 근사보다 **쉬우므로** 참값은 이보다 나쁠 가능성이 크고, 그래서 방향(초선형)은 살 것 같지만 **측정된 적은 없습니다** |

블록 합성 `U(θ; Δt)^k` 는 깊이가 T에 **선형**이라 Trotter와 같은 스케일링이고
상수배만 법니다. 스케일링 주장을 하려면 직접 훈련 쪽 데이터가 필요합니다.
이걸 논문에서 흐리게 쓰면 안 됩니다.

## 2026-08-31 — 지도교수용 진행 보고를 만들었습니다

4×4 (T1) 전체를 한 문서로 정리했습니다. 아티팩트로 발행돼 있고, 원본은
`scratchpad/report_src.html` + `build.py` (이미지를 data URI 로 인라인) 입니다.
**저장소에 남는 것은 그림과 그림 스크립트뿐**입니다.

새로 그린 그림 셋 — `scripts/plot_report_4x4.py`, 전부 커밋된 JSON 에서만 생성:

| 파일 | 내용 | 왜 필요했나 |
|:--|:--|:--|
| `report_goal1_deployable.png` | 배포 회로(합성 BP-PPS 블록) 대 grouped S2, 2Q 축 정렬 | 기존 `goal1_hva_vs_trotter.png` 는 **ceiling** 기준이라 100큐비트에서 못 만드는 회로입니다. 논문 그림은 이쪽이어야 합니다 |
| `report_goal2_hardness.png` | 반집중 `Z`, 얽힘 `S`, 10×10 하드웨어 총 fidelity (L=2 대 L=3) | `sampling_hardness.json` 과 L=2 실측에 그림이 없었습니다 |
| `report_goal3_layers.png` | 층수 대비 격차·fidelity 에 실제 BP-PPS 점을 겹침 | "남은 격차는 훈련이 아니라 깊이" 를 한 장으로 보이는 그림 |

보고서에 쓴 판정 문구 (이 표현으로 통일):

- **목표 1 — 조건부 성립.** `T=0.5` 에서 같은 2Q 게이트로 Trotter 대비 ×2.3.
  `T≥1.0` 은 역전. "모든 `T` 에서 우위" 는 **쓰지 마세요** (ceiling 기준 문장).
- **목표 2 — 미성립, 가장 약함.** 반집중·얽힘은 `k≥4` 에서 확보. 그러나
  `t*` 미측정이라 핵심 조건이 비어 있습니다.
- **목표 3 — 부분 성립.** 훈련이 안수 상한에 도달(2.5e-4)한 것은 실제 결과.
  다만 경쟁 회로(Trotter화 단열)가 없어 **비교 주장 자체가 아직 성립 안 함**.

## 2026-08-31 (2) — 어떤 논문이 되는가

"지금 결과로 논문을 쓸 수 있나" 에 대한 정리. **"양자 우위" 논문은 안 되고,
"방법론 + 한계 규명" 논문은 됩니다.** 근거는 `01-scale-plan.md` 의
"더 긴 `T` 로 훈련하면" 절.

확보된 주장 (전부 재현 가능한 실측):

| 결과 | 수치 | 종류 |
|:--|--:|:--|
| BP-PPS 훈련이 안수의 정확한 상한에 도달 (바닥상태) | 2.5e-4 | 방법론 핵심 |
| 고정 `T` 에서 항 수가 격자 크기에 포화 | 6×6 이후 ×1.02 | **규모 확장의 근거** |
| 타겟의 독립 검증 (코드 비공유) | 6.2e-7 | 신뢰성 |
| `T=0.5` 블록이 같은 게이트 수에서 Trotter보다 정확 | ×2.3 | 제한적 우위 |
| 합성이 `t=2.5` 까지 `F>0.99` | 0.9918 | 실용 |
| 훈련 회로의 반집중·얽힘 궤적 | Z 139.5 → 2.58 | 특성 규명 |
| 하드웨어 총합에서 `L=2` 가 유리 | 0.214 대 0.115 | 설계 규칙 |
| 패리티가 `|0…0>` fidelity 를 0.5로 제한 | 구조적 | 구조 발견 |
| ~~직접 훈련 깊이가 `T` 에 초선형~~ | ~~2→3→8→실패~~ | ⚠️ **강등 (09-03).** 단일상태 목적함수 측정 — 위 표 참고 |
| 깊이 축은 독립된 우위가 아님 | 1.07× | **음의 결과** |
| 초고정밀 구간에서 S4 Trotter 우세 | 2Q>350 | **음의 결과** |

**음의 결과 셋은 빼지 말고 넣는 것이 맞습니다.** 특히 깊이 축 결과는 이 분야에서
실제로 자주 잘못 인용되는 비교입니다. 이 파일의 "결론 문장 작성 요령" ("못 찾았다고 쓰는 것도
결과" 그대로.

**제안 골격:** 목표 1·3을 본문, 목표 2는 반집중 실측까지만 싣고 `t*` 를 열린
질문으로 남기는 조건부 서술. 무게중심은 "양자 우위" 가 아니라 **"고전 훈련 회로가
어디까지 가고 어디서 멈추는가"**.

## 2026-09-03 — 번호 붙은 그림 셋을 정리했습니다

`plot_results.py` / `plot_extended.py` 의 그림을 **9장에서 4장으로** 줄이고,
남은 것 중 셋을 고쳤습니다. 판단 기준은 "이 그림이 다른 데 없는 것을 보여
주는가" 하나였습니다.

### 지운 것 — `03`, `05`, `07`, `08`, `09`

PNG·생성 코드·문서 참조를 전부 지웠습니다. **숫자는 하나도 잃지 않았습니다** —
`results_4x4.md` 의 해당 절을 "그림 없음" 으로 바꾸고 스칼라는 그대로 뒀습니다.

| 그림 | 왜 |
|:--|:--|
| `03` 국소 관측량 | 결론이 `L_XZ = 0.01494` 등 스칼라 네 줄. 사이트별 막대에서 더 읽을 것이 없음 |
| `05` 바닥상태 관측량 | 판정에 쓰이는 것은 에너지·fidelity 스칼라뿐 |
| `07` 손실 히트맵 | "중앙이 크고 코너가 작다" 한 문단이면 충분 |
| `08` 종합 | 다른 패널의 몽타주 |
| `09` 합성 fidelity | (a)(b) 가 `02` 를 같은 JSON 에서 다시 그린 것이고, **(c) 는 철회된 qiskit 깊이를 x축에 쓰면서 `4.7×` 압축비를 주석으로 달고 있었음** — 이 파일이 인용 금지한 바로 그 숫자 |

`09` 는 특히 위험했습니다. 인쇄해서 보면 "HVA 가 깊이에서 4.7배 이긴다" 로
읽히는 그림이 저장소에 남아 있었던 것입니다.

### `02` — Trotter 데이터가 `06` 과 서로 달랐습니다

`02` 는 Trotter 를 `trotter_results.json` 에서 읽고 있었는데 그건 **qiskit 의
일반 Suzuki 합성**입니다. ZZ 항이 전부 교환하는데도 항별로 대칭화해 스텝당
`2 × n_bonds` 개의 RZZ 를 뱉습니다. `06` 은 처음부터 저장소 자체 grouped 빌더로
세고 있었고요. 같은 공식·같은 스텝인데:

| `t=0.5`, 5스텝 | fidelity | 2Q |
|:--|--:|--:|
| qiskit (`02` 가 쓰던 것) | 0.99983542 | **240** |
| grouped (`06` 이 쓰던 것) | 0.99975960 | **120** |

게이트를 2배 쓰고 fidelity 를 다섯째 자리에서 버는 회로 — **게이트당으로는
순수하게 나쁩니다.** 두 그림이 "Trotter" 라는 말로 다른 회로를 가리키고 있었던
셈입니다. 이제 둘 다 `plot_results.py` 의 `grouped_fidelity()` 하나에서
나옵니다.

### `Δt` 라벨이 실제 스텝 크기가 아니었습니다

`TrotterCircuit.num_steps` 는 `⌈t/Δt⌉` 로 올림한 뒤 스텝을 `t/steps` 로 줄여
`t` 에 정확히 착지합니다. **`Δt` 는 상한이지 약수가 아닙니다.** `t=0.5` 의
"`Δt=0.2`" 는 실제로 **1/6 짜리 3스텝**이고, `t=0.1` 에서는 `Δt=0.1` 과 아예
같은 회로입니다 (그래서 두 곡선이 거기서 겹쳤습니다).

게이트 수가 `3 × 24 = 72` 로 HVA 3층과 동수라 **비교 자체는 공정했고 `×2.3` 은
유효**합니다. 틀린 건 숫자가 아니라 라벨이었습니다. 그림·문서 모두 `Δt ≤ x` 로
바꾸고 실제 스텝 수를 병기했습니다. **논문에서도 `Δt = 0.2` 라고 쓰지 마세요.**

### `S₄` 를 넣었습니다 — 다만 비용을 같이 보여야 합니다

이 격자에서 `S₄` 는 사실상 exact 입니다 (`1-F ~ 1e-7`). 대신 스텝당 `S₂` 5개라
게이트가 5배입니다. **시간 축만 있는 그림에 올리면 공짜처럼 보입니다** — 그래서
`02` 는 범례에 각 곡선의 2Q 비용을 박아 넣었고, 비용을 맞춘 비교는 `06(c)` 로
옮겼습니다.

### `06(c)` 를 "시간 대 정확도" 에서 "비용 대 정확도" 로 바꿨습니다

예전 (c) 는 infidelity 대 시간이라 **`02(b)` 를 다시 그린 것**이었습니다 —
`09` 를 지운 이유와 똑같은 중복입니다. 게다가 같은 `t` 에서 곡선들의 게이트
수가 전혀 다른데 세로로 비교하게 만듭니다. 목표 1의 주장은 **비용을 맞춘**
비교에 걸려 있으므로, `t=0.5` 를 고정하고 x축을 2Q 게이트 수로 바꿨습니다.
`S₂` 1~12스텝, `S₄` 1~4스텝 위에 BP-PPS `L=2`·`L=3` 점을 얹습니다.

그림이 보여 주는 것 (전부 `|0…0>` 기준):

| `t=0.5`, 72 2Q | `1-F` |
|:--|--:|
| BP-PPS `L=3` | **8.18e-04** |
| grouped `S₂` 3스텝 | 1.91e-03 |

**2.33배**, 이 그림에서 정확히 동수인 유일한 점입니다. `L=2` (48 2Q) 는 같은
예산의 `S₂` 2스텝에 **2배 집니다.** `S₄` 곡선은 약 200 2Q 에서 `S₂` 를 가로질러
그 위 초고정밀 구간을 가져갑니다 — 이 파일 표의 "초고정밀 구간에서 S4 우세"
음의 결과가 이제 그림 한 장으로 보입니다.

### `04(b)` 에 중단된 `L=5` 실행을 겹쳤습니다

41시간짜리 `L=5` 바닥상태 실행의 에너지가 `gs_L5_aborted.json` 에 복구돼 있어
(`06a07ae`, 데스크탑) 그것을 패널에 올렸습니다. 각도는 사라져서 회로로는
못 쓰지만 에너지는 유효합니다.

| | `L=3` (생산) | `L=5` (중단, Adam 만) |
|:--|--:|--:|
| 최종 에너지 | -21.9993 | **-22.1926** |
| `E₀` 격차 | 0.4729 | **0.2796** |

**이건 목표 3 문구에 직접 걸립니다.** 이 파일 표의 "바닥상태 준비 우위 — 3층
`F0=0.7211`, 격차 0.4729" 를 읽는 사람은 0.4729 가 BP-PPS 의 한계라고
생각하게 되는데, **아닙니다. 3층 안수의 한계입니다.** 5층이면 정제 없이 Adam
만으로 0.28 에 닿습니다. 목표 3의 남은 장애물은 여전히 경쟁 회로 곡선이지만,
"우리 쪽 격차가 크다" 는 반론은 이걸로 답이 됩니다.

### 남은 것

- `report_goal1_deployable.png` 은 아직 **철회된 ceiling 곡선**을 세 패널 모두에
  그립니다 (`a45a88a` 이전인 8/31 19:50 생성). 보고서에 다시 쓰기 전에 다시
  그려야 합니다. 번호 그림이 아니라 이번 작업 범위 밖이었습니다.
- `04(a)` 의 `L_XZ` 곡선과 `03` 절의 절단 추정 관계는 그대로입니다.

## 2026-09-03 (2) — 보고서 그림 다섯 장

### `report_goal1_deployable.png` — ceiling 곡선 제거

세 패널 전부에서 뺐습니다. `a45a88a` 가 철회한 것을 그림만 못 따라가고 있었고,
`T=0.5` 에서 `3e-7` 까지 내려가며 그림을 지배해 **배포 가능한 회로들이 100배
나빠 보이게** 만들고 있었습니다. 빼고 나니 목표 1의 실제 모양이 드러납니다:

| | `T=0.5` | `T=1.0` | `T=2.0` |
|:--|:--|:--|:--|
| BP-PPS `L=3` | Trotter 곡선 **아래** (승) | 곡선 **위** (무승부~약간 패) | 곡선 위 (패) |
| BP-PPS `L=2` | 곡선 위 (패) | 곡선 위 (패) | 곡선 위 (패) |

**"모든 `T` 에서 우위" 는 물론이고 "`T` 가 커져도 버틴다" 도 쓸 수 없습니다.**
이긴 점은 `T=0.5`, `L=3` 하나입니다.

### `report_goal3_layers.png` — `L=5` 를 넣었습니다

바닥상태 ceiling 은 **철회 대상이 아닙니다.** `a45a88a` 가 죽인 것은 시간진화
쪽이고, 그건 pilot 이 `|0…0>` 하나에 각도를 맞춰 그 상태의 궤적을 보간해
버렸기 때문입니다. 에너지는 단일 상태 양이 아니므로 바닥상태 pilot 은
**같은 안수가 낼 수 있는 최선**이 맞습니다.

| `L` | ceiling 격차 | 실제 BP-PPS 격차 | |
|--:|--:|--:|:--|
| 3 | 0.4726 | 0.4729 | ceiling 도달 (2.5e-4) |
| 5 | **0.0419** | **0.2796** | **6.7배 미달** (중단) |

`L=5` 점을 넣은 이유: 넣지 않으면 "BP-PPS 가 ceiling 에 도달한다" 가 **모든
깊이에서 성립하는 것처럼 읽힙니다.** 실제로 확인된 것은 `L=3` 하나입니다.
다만 그 `L=5` 실행은 41시간 만에 L-BFGS-B 안에서 죽어 Adam 만 끝난 상태라
**"도달 못 한다" 의 증거가 아니라 미완 최적화의 하한**입니다 — 그림에도 그렇게
적어 뒀습니다. 패널 (b) 에는 `L=5` 마커가 없습니다. fidelity 는 상태가 있어야
하는데 각도가 사라졌기 때문입니다.

제목도 "lands on the exact ceiling" 에서 **"that is established at `L`=3 only"**
로 바꿨습니다.

### `goal3_energy_vs_2q.png` — 삭제

`report_goal3_layers.png` 패널 (a) 와 **완전히 같은 그림**이었습니다 (같은
ceiling, 같은 `L=3` 마커, 같은 축). `plot_pilot_comparison.py::plot_goal3` 는
이제 이유를 적고 `SystemExit` 을 냅니다. 이 파일의 두 그림이 모두 철회
상태가 됐습니다.

### `report_depth_audit.png` — `06(a)` 와 모순되지 않습니다 (재확인)

`06(a)` 의 해석적 깊이가 감사에서 **ASAP 스케줄러로 실측한 값과 정확히
일치**하는지 대조했습니다:

| 회로 | 감사 실측 | `06` 해석식 | |
|:--|:--|:--|:--|
| HVA | `5 × units` (5,10,15,…) | `5·L·k` | ✅ |
| grouped `S₂` | `5 × units + 1` (6,11,16,…) | `5·steps + 1` | ✅ |
| grouped `S₄` | `25 × units + 1` (26,51,76,101) | `5·(5·steps) + 1` | ✅ |

전부 맞습니다. 두 그림은 **x축이 다를 뿐**입니다 — `06(a)` 는 *시간* 고정 축,
감사 (a) 는 *게이트 수* 축. 같은 `t` 에서 `S₄` 가 5배 깊은 것과, 같은 게이트
수에서 셋이 한 직선에 놓이는 것은 둘 다 참입니다.

**다만 `06(a)` 는 `06(b)` 를 상수 `5/24` 로 다시 그린 것**이고 (계열별 offset
최대 1층), 저도 패널에 그렇게 적어 뒀습니다. 감사 그림이 실측 스케줄러로 그
논증을 이미 엄밀하게 하고 있으므로, **`06` 을 두 패널로 줄이는 것이 09·06(c)
에 적용한 기준과 일관됩니다.** 아직 안 했습니다 — 판단 필요.

### `report_goal2_hardness.png` — 반집중은 **학습된 것이 아닙니다**

이게 이 그림에서 가장 오해받기 쉬운 지점이라 문서에 못박습니다.

훈련 목적함수는 `L_XZ` — 단일 사이트 `X`/`Z` 기댓값을 `exp(-iHt)` 의 것과
맞추는 것뿐입니다. **반집중이나 얽힘에 대한 훈련 압력은 어디에도 없습니다.**
(a)(b) 가 보여 주는 것은 회로가 근사하는 **물리적 시간진화 상태 자체**의
성질입니다. 해밀토니안의 실제 동역학이 곱상태를 스크램블하는 것이고, 회로는
그걸 따라가고 있을 뿐입니다.

그 증거가 그림 안에 있습니다: **`L=2` 와 `L=3` 곡선이 (a)(b) 에서 거의
겹칩니다.** 안수 깊이를 1.5배로 늘려도 반집중·얽힘이 변하지 않는다는 것은,
그것이 안수의 성질이 아니라 타겟 상태의 성질이라는 뜻입니다. 그리고 이것이
`L=2` 를 쓰는 근거입니다 — 같은 "어려움" 을 게이트 2/3 로 삽니다.

- **(a) 반집중**: `Z = 2^n Σ_x p(x)²`. `Z=2` 가 Porter–Thomas (Haar). `k=1` 은
  `Z=139.5` 로 극도로 뾰족해 목표 2에 못 씁니다. `k≥4` 에서 `Z≈2.6` 으로
  Porter–Thomas 에 근접합니다.
- **(b) 얽힘**: 절반 절단 엔트로피가 Haar 값 7.28비트(최대 8비트)로 포화.
  `k≈6` 이후 평평합니다.
- **(c)** 는 **10×10 투영**입니다 — 4×4 에서 잰 알고리즘 fidelity 를 그대로
  쓰고, 거기에 하드웨어 항 `exp(-n_2Q · ε)`, `ε=1e-3`, `n_2Q = 180·L·k` 를
  곱합니다. 결론은 **하드웨어 지수항이 알고리즘 차이를 압도하므로 `L=2` 가
  총합에서 이긴다**는 것.
  ⚠️ **알고리즘 fidelity 를 4×4 에서 10×10 으로 그대로 옮긴 것은 측정이 아니라
  외삽입니다.** 논문에 쓸 때 반드시 그렇게 표기해야 합니다.

### `report_noise_budget.png` — 제목이 관측량 이야기를 샘플링 이야기로 읽히게 했습니다

세 패널:

- **(a)** 단일 사이트 관측량의 역방향 광원뿔 안에 든 2Q 게이트 비율. 10×10,
  `L=2` 에서 `t=0.5` 면 회로의 **27%** 만 그 관측량에 닿을 수 있고, `t=4` 면
  90%. 4×4 는 `t=0.5` 에 이미 82% 라 **작은 격자에서는 광원뿔이 절약이 안
  됩니다.**
- **(b)** 그 절약분. `F_global = exp(-n_total·ε)` 대 `F_local = exp(-n_cone·ε)`.
  10×10 `L=2` 에서 약 2.4배.
- **(c)** 선형 XEB 로 `F` 를 10% 정밀도로 **추정**하는 데 필요한 샷 수,
  `M = 100/F²`.

**"샷을 많이 쏘면 정확한 샘플링을 얻을 수 있나?" — 아닙니다.** 제목이 그렇게
읽히게 돼 있었고 (`averaging recovers the signal cheaply`), 고쳤습니다.

- **관측량이면 됩니다.** 탈분극 노이즈는 `<O>` 를 대략 `F` 배 합니다. `F` 를
  알면 되돌릴 수 있고 대가는 분산뿐이라 `1/F²` 샷입니다. 이게 (c) 가 세는
  것이고 실제로 쓰는 기법입니다. **목표 1이 여기 삽니다** — 10×10 에서
  `t≈2` 까지 `ε=3e-3` 이면 5.6e5 샷으로 예산 안입니다.
- **샘플링이면 안 됩니다.** 확률분포에는 되돌릴 스케일 인자가 없습니다.
  10×10 `L=2` `t=2`, `ε=3e-3` 이면 `F=0.013` — **비트스트링의 약 99%가
  노이즈에서 나옵니다.** 샷을 아무리 늘려도 그 99%는 그대로입니다.
  **목표 2가 샘플링 주장**이므로, (c) 는 목표 2가 `t=2` 까지 산다는 뜻이
  **아닙니다.** `t=2` 에서 fidelity 를 *잴 수 있다*는 뜻입니다.

게다가 `exp(-n·ε)` 모델 자체가 낙관적입니다. 2Q 탈분극만 세고 **판독 오차,
crosstalk, coherent 누적을 전부 뺐습니다.** 100큐비트에서 큐비트당 판독 오차
1e-2 만 넣어도 게이트를 하나도 돌리기 전에 `0.99¹⁰⁰ ≈ 0.37` 입니다.

## 2026-09-03 (3) — 세 목표 현재 판정

이 세션에서 "세 목표를 다 못 맞춘 것 아니냐" 는 물음이 나왔고, 그림을 정리하며
확인된 것과 데스크탑이 올린 `8e940f4` 를 합쳐 다시 판정했습니다. **결론은
"셋 다 실패" 가 아니라 세 목표의 상태가 서로 다르다는 것**입니다.

> ⚠️ 이 절은 같은 세션 안에서 한 번 틀렸던 판정을 고친 것입니다. 목표 3을
> "경쟁 회로가 없어 판정 불가" 라고 적었는데, 그 시점에 이미 데스크탑이
> `gs_adiabatic.json` 을 올려 둔 상태였습니다 (`8e940f4`, 09-03 00:05).
> **판정 전에 `git fetch` 를 먼저 하세요.**

### 목표 1 — 쟀고, 얇게 성립

유일하게 경쟁자와 실제로 붙여 본 목표입니다.

| 측정 | 결과 |
|:--|:--|
| 상태평균, `L=3`, 72 2Q | `S₂` 대비 게이트 **23% 절감** |
| 상태평균, `L=2`, 48 2Q | Trotter 에 **2배 패배** |
| `\|0…0>`, `T=0.5` / `T=1.0` / `T=2.0` | 2.33배 승 / 무승부 / 패배 |
| 깊이 축 | 1.07배 — **우위 없음** |
| 입력 강건성 | BP-PPS 1.08배, `S₂` 1.32배, `S₄` 1.39~1.83배 |

> ⚠️ **이 표의 첫 세 줄은 아래 "2026-09-03 (6)" 이 갱신했습니다.** 당시에는
> `T`=1.0·2.0 의 곱상태 평균이 없어 그 두 시각을 `|0…0>` 으로 판단했습니다.
> 공정 지표로 다시 재니 **세 시각 모두 BP-PPS 승** (2.86 / 1.23 / 1.20배)
> 입니다. `|0…0>` 줄의 "무승부"도 실제로는 1.21배 패배입니다 — 판정에 쓰지
> 말고 **채점 기준이 결론을 바꾼다는 예시**로만 읽으세요.

"0" 은 아닙니다. 23% 절감과 1.08배 강건성은 재현 가능한 실측입니다. 다만
**`L=3`, 짧은 `T` 라는 한 조건에서의 상수배**이고, 블록 합성은 깊이가 `T` 에
선형이라 스케일링 우위가 원리적으로 없습니다. **"명확한 우위" 로는 못 씁니다.**

`8e940f4` 가 `S₄` 를 공정 지표에 올리면서 하나 더 나왔습니다: **`S₄` 는 200
게이트 아래에서 `S₂` 보다 나쁩니다.** 한 스텝이 이미 120 게이트인데 그것이
`S₂` 5스텝만큼도 못 삽니다. ⚠️ 지금 `02`·`06` 의 `S₄` 곡선은 `\|0…0>` 기준이라
`S₄` 를 실제보다 좋게 보이게 합니다 — 논문 그림은 상태평균으로 다시 그려야
합니다.

### 목표 2 — 반증이 아니라 **미측정**, 그리고 하드웨어에서 죽음

`t*` (고전 방법이 무너지는 시각) 를 **한 번도 재지 않았습니다.** 이게 없으면
주장의 주어가 비어 있습니다. 확보한 반집중·얽힘은 **학습된 것이 아니라 타겟
상태의 성질**입니다 (`L=2`·`L=3` 곡선이 겹치는 것이 증거).

하드웨어 쪽은 `ε` 에 따라 갈립니다. 10×10, `L=2`, `t=2`:

| `ε` | `F_global` |
|:--|--:|
| 3e-3 | **0.013** |
| 1e-3 | 0.237 |

관측량이면 `ε=1e-3` 의 0.24 는 되돌려 쓸 수 있습니다. **샘플링이면 둘 다
죽습니다** — 분포에는 되돌릴 인자가 없습니다. 목표 2가 샘플링 주장이라
여기서 끝나고, 이 `exp(-nε)` 는 판독 오차조차 안 센 낙관값입니다.

**셋 중 가장 약합니다.**

### 목표 3 — 경쟁 곡선이 생겼고, **셋 중 가장 강합니다**

`8e940f4` 가 4×4 단열 스윕을 올렸습니다 (선형 스케줄, `Γ=1`, `T_A` 0.5~40 중
최선, `M` 을 32까지 실측).

| | 2Q | 격차 `E-E₀` | `F₀` |
|:--|--:|--:|--:|
| **BP-PPS `L=3` (실측)** | **72** | **0.4729** | **0.7211** |
| 단열 최선 실측 (`M`=32) | 768 | 0.6651 | 0.6828 |

**72 게이트가 768 게이트를 양쪽 지표에서 이깁니다.** 10배 이상이고 양쪽 다
실측입니다 — 목표 3이 하려던 "같은 상태를 더 얕은 회로로" 가 처음으로 증거를
가졌습니다.

스케일링도 유리해 보입니다: 격차가 단열은 `2Q^-0.94` (사실상 게이트 수분의
1), HVA ceiling 은 `2Q^-3.60`. 같은 에너지에 필요한 게이트 배수는

| | `L=2` | `L=3` | `L=4` | `L=5` | `L=8` |
|:--|--:|--:|--:|--:|--:|
| 단열 / HVA | 5.0× | 15.3× | 40.4× | 121× | 253× |

> **논문에 쓸 문장은 이것입니다: "BP-PPS 72 2Q 가 우리가 측정한 가장 깊은
> 단열 회로(768 2Q)를 이긴다 — 10.7배 이상, 양쪽 다 실측."**
> ⚠️ **"768" 은 곡선의 끝점이지 안쪽 점이 아닙니다** (아래 엔진 세션 항목 2).
> 그래서 "단열을 이긴다" 가 아니라 **"측정한 범위에서 이긴다"** 로 써야 하고,
> `03f_gs_competitors.py` 를 `M=100`(2400 2Q)까지 돌리면(6분) 이 단서가
> 사라집니다. 돌리기 전에는 끝점이라는 사실을 문장에 남겨 두세요.
> `01-scale-plan.md` (`50fac2a`) 는 같은 결과를 **15~259배**로 적었는데 그건
> 위 외삽표의 범위입니다 — 두 파일이 다투는 것이 아니라 하나는 외삽 범위,
> 하나는 실측 하한입니다. **본문에는 실측 하한을, 전망에는 외삽을** 쓰세요.

**단, `L=2` 만 단열 측정 범위(768 2Q) 안입니다.** 나머지는 외삽이고, 더 중요한
건 `L≥4` 의 배수가 **BP-PPS 가 그 깊이의 ceiling 에 도달한다는 것을 전제**한다는
점입니다. 그런데 유일한 `L=5` 시도는 ceiling 대비 6.7배 미달로 중단됐습니다.
**큰 배수들은 아직 우리 것이 아닙니다.**

남은 약점 셋:
1. 상대가 **선형 스케줄** 단열입니다. local adiabatic 등 더 나은 스케줄이면
   `2Q^-0.94` 가 개선됩니다. 리뷰어가 반드시 지적합니다.
2. QAOA 는 아직 없습니다 — 무작위 시작에서 `p=4` 0.96, `p=10` 0.87 로 정체
   (알려진 large-`p` 실패 모드). 단열 시드 스윕이 데스크탑 대기 중.
3. **훈련 비용**이 진짜 벽입니다. 16큐비트 5층에 Adam 19.9시간 + L-BFGS-B
   21시간, 그러고도 미수렴. 이건 버그가 아니라 곡선이라 100큐비트에 그대로
   올리면 답이 안 나옵니다.

### 그래서 어떤 논문인가 — 08-31 결론 유지

**"양자 우위" 논문은 안 되고 "방법론 + 한계 규명" 논문은 됩니다.** 다만 목표 3
쪽 무게가 8-31 때보다 늘었습니다. 무게중심 후보는 이제 둘입니다:

- **(A) 한계 규명 중심** — 음의 결과 셋(깊이 축 무우위, 초선형 깊이,
  단일상태 과적합 30000배, `S₄` 저게이트 열세)을 본문으로. 목표 1·2는 조건부.
- **(B) 목표 3 중심** — "고전 훈련한 얕은 회로가 Trotter화 단열보다 10배 얕게
  같은 바닥상태에 간다" 를 주장으로 삼고, 목표 1·2를 부차로. **`L=5` 재훈련이
  성공해야 성립**하고, 성공하면 훨씬 강한 논문입니다.

(B) 를 노린다면 다음 한 수는 그림이 아니라 **`L=5` 훈련을 끝내는 것**입니다.

## 2026-09-03 (5) — 깊이 그림 교체, 그리고 반집중 no-go 논문과의 관계

### `report_depth_audit.png` → `report_depth_fidelity.png`

옛 그림의 패널 (b) 가 **읽히지 않는다**는 지적을 받았고, 맞습니다. 그 막대
차트는 "4.67배 → 1.07배" 를 보여 주려 했는데:

| | 왼쪽 쌍 (옛 그림) | 오른쪽 쌍 (수정 후) |
|:--|:--|:--|
| HVA | 72 2Q, 해석적 4색 깊이 15 | 72 2Q, ASAP 깊이 15 |
| Trotter | **240 2Q**, qiskit 미정렬 깊이 70 | **72 2Q**, ASAP 깊이 16 |

**두 막대는 같은 비교가 변한 것이 아니라 아예 다른 비교였습니다.** 왼쪽은
게이트 수도 다르고(72 대 240) 깊이 규약도 다릅니다. 즉 **게이트 2배 부풀림**과
**스케줄링 누락**이라는 독립된 두 오류가 한 숫자로 뭉개져 있어서, 어느 쪽이
얼마나 기여했는지 그림에서 볼 방법이 없었습니다.

대체 그림은 질문에 직접 답합니다 — **y축 infidelity, x축 스케줄된 깊이**,
`T=0.5` 고정. 그리고 **상태평균 지표**를 씁니다 (열린 질문에 있던 항목 해소).

| `T=0.5` | 깊이 | 2Q | 상태평균 `1-F` |
|:--|--:|--:|--:|
| BP-PPS `L=3` | **15** | 72 | **8.81e-04** |
| grouped `S₂` 3스텝 | **16** | 72 | 2.52e-03 |

깊이 1.07배 차이에서 정확도 **2.9배**. 패널 (b) 가 같은 데이터를 게이트 수 축에
다시 그리는데 **모양이 같습니다** — 여기 모든 회로가 `깊이 = 5×(2Q/24)` 를
따르기 때문이고, 그래서 **깊이는 독립된 축이 아닙니다.** 옛 감사 그림의 진짜
결론이 막대 차트 없이 이 한 문장으로 남습니다.

상태평균으로 바꾸면서 `S₄` 가 제자리를 찾았습니다: **약 200게이트 아래에서
`S₂` 에 집니다** (`\|0…0>` 기준 그림은 `S₄` 를 실제보다 좋게 보입니다).

### 반집중 — *Limits of quantum generative models with classical sampling hardness* (arXiv:2512.24801)

"그 논문은 안 된다고 하는데 우리는 되는가" 라는 물음. **모순되지 않습니다.
설정이 다릅니다.**

그 논문의 no-go 는 **생성 모델링**(generative modelling)에 대한 것입니다 —
데이터로 주어진 타겟 분포를 회로가 흉내 내도록 훈련하는 문제. 결론은
*"타겟이 반집중하면 분포 기반 손실의 gradient 가 계 크기에 지수적으로
사라져 훈련이 안 된다"* 입니다. **회로가 반집중 분포를 만들 수 없다는 말이
아닙니다** — 논문도 그 구분을 명시합니다.

우리 설정이 다른 지점 셋:

1. **분포를 훈련하지 않습니다.** BP-PPS 목적함수는 `L_XZ` — 단일 사이트
   `X`/`Z` **기댓값**을 맞춥니다. KL·MMD 같은 분포 손실이 아니고, 논문의
   barren gradient 논증은 그 분포 손실에 붙습니다.
2. **타겟이 데이터가 아니라 물리적으로 정의**됩니다 (`exp(-iHt)|ψ₀>`).
   타겟 분포를 샘플로 추정할 일이 없습니다.
3. **반집중은 훈련 대상이 아니라 사후 측정값**입니다. `L=2` 와 `L=3` 곡선이
   겹치는 것이 그 증거 — 안수의 성질이 아니라 타겟 상태의 성질입니다.

**그래서 (a) 의 `Z → 2.6` 은 유효합니다.**

⚠️ **다만 그 논문의 나머지 절반은 우리를 정확히 때립니다.** 반집중은 고전
난해성의 재료인 동시에 **검증의 장벽**이라, 반집중하는 모델은 효율적으로
검증할 수 없다는 것이 논문의 두 번째 축입니다. 이건 목적함수를 뭘 쓰든
상관없이 성립하고, `report_noise_budget.png` (c) 가 수치로 보여 준 벽
(XEB 샷 `1/F²`, `F` 는 지수적 감소) 의 이론판입니다.

**정리:** 반집중 자체는 우리 주장이 맞습니다. 하지만 논문이 던지는 진짜 교훈
— *"생성 모델의 양자 우위는 반집중이 아닌 다른 데서 와야 한다"* — 는 목표 2에
그대로 적용됩니다. **반집중만으로는 목표 2가 서지 않고, 여전히 `t*` 가
필요합니다.** 논문을 인용하면서 이 구분을 안 하면 리뷰어가 바로 잡습니다.

> 근거: arXiv:2512.24801 초록과 본문 요약. **정독은 아직 안 했습니다** —
> 목표 2를 본문에 쓸 거면 검증 절을 직접 읽고 우리 XEB 수치와 대조해야
> 합니다.

## 2026-09-03 (6) — 공정 지표로 재니 목표 1이 `T`=2.0 까지 삽니다

### 정정: "이긴 점은 `T=0.5` 하나" 는 틀렸습니다

이 파일이 그동안 "`T≥1.0` 은 역전", "이긴 점은 `T=0.5`, `L=3` 하나" 라고 적어
온 것은 **전부 `|0…0>` 채점 기준**이었습니다. `state_averaged.json` 이 `T=0.5`
에만 있어서 나머지 두 시각은 `|0…0>` 숫자로 판단했던 것입니다.

`T`=1.0 과 2.0 을 같은 방식으로 재고(24개 Haar 곱상태, seed 11) 나니 뒤집힙니다.

| 같은 2Q | `T`=0.5 (72) | `T`=1.0 (144) | `T`=2.0 (288) |
|:--|--:|--:|--:|
| BP-PPS `L=3` (합성) | **8.81e-04** | **2.63e-03** | **6.57e-03** |
| grouped `S₂` 동수 | 2.52e-03 | 3.23e-03 | 7.86e-03 |
| **배수** | **2.86×** | **1.23×** | **1.20×** |
| (참고) `\|0…0>` 채점이면 | BP-PPS 승 | **Trotter 승** | **Trotter 승** |

**곱상태 평균에서는 세 시각 모두 BP-PPS 가 이깁니다.** `|0…0>` 에서는 `T≥1.0`
이 지고요. 뒤집히는 이유는 열화율입니다:

| `\|0…0>` → 평균 열화 | `T`=0.5 | `T`=1.0 | `T`=2.0 |
|:--|--:|--:|--:|
| BP-PPS (`L=3`) | 1.077 | 1.069 | **1.034** |
| grouped `S₂` (동수) | 1.323 | 1.592 | 1.708 |
| grouped `S₄` (전 구간) | 1.39~1.83 | 1.13~1.65 | 1.00~1.72 |

BP-PPS 는 `L_XZ` 라는 **연산자 거리**를 최적화하므로 입력 상태가 바뀌어도 거의
안 무너지고, Trotter 는 상태 무관 회로인데도 `|0…0>` 에서 유독 잘 나옵니다
(`|0…0>` 이 이 해밀토니안에 대해 특별히 쉬운 상태). **`|0…0>` 채점이 Trotter
쪽을 편들고 있었습니다.**

`CLAUDE.md` 의 목표 1 문구가 "**임의** 곱상태" 이므로 **평균 쪽이 맞는 지표**
입니다. 다만 논문에 쓸 때 한 가지는 정직하게 밝혀야 합니다: 평균 지표는
BP-PPS 가 최적화한 것(무한온도 연산자 거리)에 더 가깝습니다. Trotter 가
불리해서가 아니라 **채점 기준을 고르는 순간 이미 한쪽을 고른 것**이므로,
두 숫자를 **나란히** 싣는 것이 안전합니다.

### `report_depth_fidelity.png` 를 세 시각으로 확장

패널 셋(`T`=0.5, 1.0, 2.0), x축 스케줄된 깊이, y축 곱상태 평균 infidelity.
각 패널에 게이트 동수 쌍을 주석으로 답니다. `S₄` 는 세 시각 모두 저게이트
구간에서 `S₂` 에 지고, `T`=2.0 에서는 480 2Q 를 넘어야 따라잡습니다.

### `03g_state_averaged.py` 버그 하나 고쳤습니다

`--T` 옵션이 있었지만 **BP-PPS 블록을 항상 한 번만** 적용했습니다. 블록은
`dt=0.5` 에만 훈련돼 있으므로 `T=1.0` 에서는 `U(θ;0.5)²` 여야 하는데, 반쪽
시간 회로를 전체 시간 타겟에 채점해 **가짜 실패**가 나올 뻔했습니다. 이제
`k = T/0.5` 를 세어 합성하고, `T` 가 0.5 의 배수가 아니면 거부합니다.
`T=0.5` 결과는 비트 단위로 동일합니다 (검증함).

### "초선형 깊이" 를 강등했습니다

"직접 훈련 깊이가 `T` 에 초선형(2→3→8→도달못함)" 은 `statevector_pilot.json`
의 `time_evolution` 에서 나왔는데, **그건 `a45a88a` 가 철회한 바로 그 최적화**
입니다 — 각도를 `|0…0>` 하나에 맞춘 것이라 `T=0.5`, `L=3` 에서 `|0…0>`
infidelity 1.4e-5 인 회로가 곱상태 평균으로는 **0.43** 입니다.

"statevector 라서 부정확한 것" 은 **아닙니다.** `2^16` 정확 시뮬레이션이라
계산 자체는 정확합니다. 틀린 것은 **잰 대상**입니다 — 한 상태의 궤적을
보간하는 데 필요한 층수이지, `exp(-iHT)` 를 근사하는 데 필요한 층수가
아닙니다.

부수적으로 확인한 것: 임계점 세 개(`T`=0.5→`L`=2, 1.0→`L`=3, 2.0→`L`=8)는
전부 **수렴한** 점입니다 (85 s / 135 s / 305 s, `max_seconds` 미도달). 그래서
시간 예산 문제는 아니고 오직 목적함수 문제입니다. 반면 `T`=4.0 의
"도달못함" 은 `L`=12·16 이 1800 s 에 잘린 것이라 **그쪽은 두 문제가 겹칩니다.**

**방향(초선형)은 살아남을 가능성이 큽니다** — 단일상태 맞춤이 연산자 근사보다
쉬우므로 참값은 이보다 나쁠 것입니다. 하지만 **측정된 적은 없고**, 제대로
재려면 각 `(T, L)` 에서 목적함수를 곱상태 평균이나 `L_XZ` 로 바꿔 다시
최적화해야 합니다. 싸지 않습니다. **그때까지 "2→3→8" 을 인용하지 마세요.**

## 2026-09-03 (7) — 표기 결정 (02 세션에서 넘어옴, 본체는 `02-comparison-models.md`)

- **경쟁자 표기는 `VQE (층당 2각)`. `QAOA` 라고 쓰지 않습니다.** 회로 모양만
  QAOA 이고 최소화하는 것은 횡자기장을 포함한 `H` 의 에너지라 목표물이
  비트스트링이 아닙니다. `gs_competitors.json` 의 키는 `qaoa` 그대로이고
  (데이터 파일은 사후에 안 고침) **키 `qaoa` = 문서의 VQE** 입니다.
- **우리 안수는 계속 `HVA`.** 시간 진화 쪽에서 이미 그렇게 부르고 있어서
  목표별로 이름이 갈리는 게 더 나쁩니다. 다만 층당 40 자유각이라는 점에서
  문헌의 multi-angle QAOA (Herrman et al., *Sci. Rep.* 12, 6781 (2022)) 와
  같은 파라미터화이고, **"HVA 가 VQE 를 이긴다"는 그 문헌이 이미 보고한
  다중각도 대 단일각도 결과와 같은 크기**입니다. 새 결과로 주장하면 안 되고,
  어디서 이 동일성을 언급할지는 나중에 정하기로 했습니다.
- 목표 3 숫자를 쓸 때 **층위 A(안수 상한, 배포 불가) / 층위 B(BP-PPS 훈련
  완료, 배포 가능)** 를 구분할 것. 대표 문장은 층위 B: 72 2Q, `dE`=0.4729,
  단열 대비 15.0배, VQE 대비 1.8배.

## 열린 질문

- ~~목표 3을 논문에 넣을지, 뺄지, "예비 결과"로 넣을지~~ **넣습니다 (09-03).**
  단열 곡선이 생겼고 `L=3` 에서 10배 이상 우위가 실측입니다. 이제 질문이
  바뀌었습니다: **본문 주장으로 삼을지(위 (B)), 부차로 둘지(위 (A))** — 그리고
  그건 `L=5` 재훈련이 끝나야 답할 수 있습니다.
- **논문 무게중심을 (A) 한계 규명 / (B) 목표 3 중 어디에 둘지.** 지금 정하지
  말고 `L=5` 결과를 보고 정하는 것이 맞습니다.
- 어느 학회/저널을 겨냥하는지에 따라 필요한 규모가 달라집니다.
- ~~위 표의 "제한적 우위" 두 줄은 무작위 초기상태 재측정 결과에 따라 바뀝니다~~
  **해소됨 (2026-08-31, `de9f584`/`a45a88a`).** 24개 무작위 곱상태로 다시 쟀고,
  **우위는 넓어지지 않았습니다.** `L=3` 에서 게이트 23% 절감, `L=2` 에서는
  Trotter 에 2배 패배. 즉 우위는 `L=3` 에만 있고 얇습니다. 본문 문구를
  확정해도 되지만, 확정할 문구가 "얇은 조건부 우위" 라는 뜻입니다.
- **본문 목표 1 그림을 `report_goal1_deployable.png` 로 할지 `06(c)` 로 할지.**
  ceiling 은 제거됐으므로 이제 둘 다 유효하고 서로 보완적입니다 — 전자는 세
  시간 `T` 를, 후자는 `t=0.5` 한 시간에서 더 촘촘한 스텝 스윕을 보여 줍니다.
  둘 다 싣거나, 전자만 싣고 후자를 부록으로.
- **`06(a)` 를 뺄지.** `06(b)` 를 상수 `5/24` 로 다시 그린 것이고,
  `report_depth_fidelity.png` 가 실측 확인된 깊이로 같은 논증을 더 강하게 합니다.
  09 와 `06(c)` 에 적용한 기준을 그대로 적용하면 빼는 것이 맞습니다.
- **`06(c)` 를 `t=0.5` 외의 시간에서도 그릴지, `L=2·3` 외의 층수를 넣을지.**
  둘 다 새 훈련이 필요합니다 (2026-09-03 확인, 나중으로 미룸).
- **`report_goal2_hardness.png` (c) 의 10×10 알고리즘 fidelity 는 4×4 외삽**
  입니다. 논문에 실으려면 표기하거나 10×10 에서 실측해야 합니다.
- **`02`·`06` 의 `S₄` 곡선을 상태평균으로 다시 그릴지.** 지금은 `\|0…0>` 기준
  이라 `S₄` 가 실제보다 좋아 보입니다 — 공정 지표에서는 200 게이트 아래에서
  `S₂` 에 집니다 (`state_averaged.json`, `8e940f4`).
  `report_depth_fidelity.png` 는 이미 상태평균으로 그렸으므로, 번호 그림 둘만
  남았습니다.
- **목표 2를 본문에 쓸 경우 arXiv:2512.24801 의 검증 절을 정독할 것.** 반집중
  쪽은 설정이 달라 모순이 없지만 **검증 장벽은 우리에게 그대로 적용**되고,
  인용하면서 그 구분을 빠뜨리면 리뷰어가 지적합니다.
- **단열 상대를 선형 스케줄보다 강하게 할지.** 지금 목표 3 우위의 크기가
  여기 달려 있고, 리뷰어가 가장 먼저 볼 지점입니다.

## 2026-09-03 (4) — 엔진 세션에서 넘어온 것 (본체는 `03-engine-performance.md`)

담당은 `03` 세션이었습니다. 여기에는 **주장 문구에 걸리는 것만** 옮깁니다.

**1. 커밋된 숫자는 하나도 안 움직였습니다.** 엔진 세 군데가 빨라졌지만
(타겟 스캔 120배, `np.union1d` 5.9배, `np.exp` 1.8배) 전부 **출력이 비트 단위로
동일**하거나(전자 둘은 최대편차 정확히 `0.0`) 합산 순서 차이 `1e-14` 수준이고,
문자열 오라클·qiskit·중앙차분 대조를 통과했습니다. 위 "쓸 수 있는 것 / 없는
것" 표의 모든 수치가 그대로 유효하고 **재훈련이 필요 없습니다.**

**2. 단열 곡선이 우리가 인용하는 바로 그 점에서 끝나 있습니다.** 목표 3의 대표
문장이 쓰는 단열 **768 2Q** 는 `gs_adiabatic.json` 의 **마지막 행**입니다 — 안쪽
점이 아니라 곡선의 끝. 데이터를 만드는 `03f_gs_competitors.py` 는 `M=100`
(2400 2Q)까지 스윕하도록 쓰여 있으므로 **그 너머를 우리는 아직 모릅니다.**
"단열을 더 깊게 하면?" 은 이 파일이 이미 마지막 열린 질문으로 적어 둔
그대로인데, 새로운 사실은 **답하는 비용이 6분**이라는 것입니다.

**3. "경쟁 회로 곡선이 없다" 의 정체는 `results/4x4/gs_competitors.json` 이
존재하지 않는다는 것입니다.** 스크립트는 커밋돼 있고(`8d450cb`) 단열·QAOA·HVA
를 같은 게이트 알파벳에서 재도록 설계돼 있는데 한 번도 완주한 적이 없습니다.
비용은 약 1시간 20분. 위 "(B) 를 노린다면 다음 한 수는 `L=5` 훈련" 은 반만
맞습니다 — **다음 한 수는 둘**입니다.

**4. 경쟁자가 미수렴 상태로 기록될 예정입니다.** 포인트당 시간 예산 때문에
`L≥8` 에서 L-BFGS 가 `maxiter=300` 에 못 닿습니다 (`L=8` 약 204, `L=12` 약 131).
**경쟁자를 덜 최적화해서 이기는 그림**이 되므로 특히 위험합니다. 이 실행 전에는
`L≤5` 범위에서만 비교 문장을 쓰세요.

### 열린 질문에 추가

- **`03f_gs_competitors.py` 를 완주시킬 것.** 목표 3 (B) 노선의 전제. 단열은
  `M=100` 까지, QAOA·HVA 는 포인트당 시간 예산을 올려서. 데스크탑 약 1시간
  20분. 완주 전에는 `L≤5` 밖의 경쟁 비교 문장을 쓰지 않습니다.
- **경쟁자 미수렴 여부를 JSON 에 남길 것.** 지금은 `maxiter` 에 닿았는지 시간에
  잘렸는지가 기록되지 않아, 나중에 그림만 보면 구분이 안 됩니다.
